# AWS Lambda CRUD Service

**Ground Truth**: AWS Lambda CRUD service for Users with DynamoDB backend

## Architecture
- Lambda handlers: handler.ts
- DynamoDB layer: db.ts
- RESTful CRUD: create, read, update, delete

## Dependencies
- aws-sdk (DynamoDB client)
- uuid (ID generation)

## Project Type
Cloud-native serverless architecture
